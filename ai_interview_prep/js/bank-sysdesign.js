/* ============================================================
   System design — the whiteboard round.
   These are long-form; treat each as a 30-45 minute question.
   ============================================================ */
window.QB = (window.QB || []).concat([

{ id: 'sd01', topic: 'sysdesign', level: 2,
  q: 'How do you approach an AI system design question at all?',
  lay: 'Ask what good looks like, how many people will use it, how fast it has to be, and what data exists — before drawing a single box. Most bad designs come from skipping those four questions.',
  tech: 'A structure that works: <ol><li><b>Clarify</b> — what counts as a good answer, who the users are, what the volume is, what latency budget, what data exists, what the failure cost is.</li><li><b>Define success metrics</b> — one primary, plus guardrails (latency, cost, safety).</li><li><b>Sketch the simplest thing that could work</b>, and say so explicitly. Then say what would break first.</li><li><b>Do the arithmetic</b> — requests per second, tokens per request, cost per request, storage.</li><li><b>Design the data path</b> before the model path — where the knowledge comes from and how it stays fresh.</li><li><b>Add the failure branches</b> — empty retrieval, timeout, rate limit, budget.</li><li><b>Evaluation and rollout</b> — how you know it works and how you ship it safely.</li><li><b>Name the trade-offs you chose</b> and what you would do differently at 10× scale.</li></ol>',
  trap: 'Doing the arithmetic out loud is the single strongest signal. "20 requests per second × 4,000 input tokens × $3 per million is about $2.20 a minute, so $95,000 a month — that changes the design" is what senior sounds like.',
  tags: ['process', 'design'] },

{ id: 'sd02', topic: 'sysdesign', level: 2,
  q: 'Design a customer support assistant for a SaaS product.',
  lay: 'Search the help centre and the customer\'s own account, answer with citations, hand over to a human when unsure, and measure how many tickets it actually prevented.',
  tech: '<b>Data:</b> help centre, changelogs, past resolved tickets (the highest-value corpus and the one people forget), and account state via API rather than the index. <b>Ingestion:</b> event-driven, chunked by document structure with the heading path, metadata for product area, version, locale and tenant. <b>Retrieval:</b> hybrid dense + BM25 with RRF, metadata filter for the customer\'s plan and product version, rerank the top 100 to the top 5. <b>Generation:</b> grounded answer with citations, explicit refusal instruction, structured output containing the answer, sources and a confidence signal. <b>Escalation:</b> on empty or low-confidence retrieval, on citation-verification failure, on an explicit user request, or on a sensitive intent (billing dispute, cancellation). <b>Live data:</b> account state as a tool call, never in the index. <b>Metrics:</b> deflection rate, escalation rate, follow-up rephrase rate, CSAT on assisted tickets, cost per deflected ticket.',
  dgm: { nodes: ['question', 'intent router', { t: 'hybrid retrieve', s: 'filtered by tenant + plan' }, 'rerank', { t: 'account tools', s: 'live state', k: 'alt' }, 'answer + cite', { t: 'verify or escalate', s: 'named branch', k: 'warn' }],
    cap: 'Live state comes from tools; documents come from the index. Never mix the two.' },
  trap: 'Resolved tickets are the best corpus and the hardest to use: they contain PII, they contain wrong answers from before a fix shipped, and they are written in shorthand. Curate them rather than dumping them in.',
  tags: ['rag', 'support'] },

{ id: 'sd03', topic: 'sysdesign', level: 3,
  q: 'Design a document Q&A system for 50 million enterprise documents.',
  lay: 'At that size the hard parts stop being "how do I search" and become "where does it all live, how does it stay current, who is allowed to see what, and what does one question cost".',
  tech: '<b>Storage arithmetic first:</b> 50M documents at ~10 chunks each is 500M vectors; at 768 float32 dimensions that is ~1.5 TB raw, so compression (PQ to ~100 bytes/vector → ~50 GB) and sharding are mandatory. <b>Ingestion:</b> streaming CDC, format-routed parsing, content hashing, idempotent upserts, delete propagation, reconciliation, freshness SLO. <b>Index:</b> sharded by tenant where possible (one query touches one shard), replicated; hybrid dense + BM25; metadata including ACLs. <b>Retrieval:</b> permission filter FIRST (it cuts the space by orders of magnitude), then hybrid, then rerank. <b>Serving:</b> semantic cache with tenant-scoped keys, prompt caching, model router. <b>Governance:</b> per-tenant isolation tested in CI, audit logging, deletion path. <b>Evaluation:</b> a frozen eval set per major tenant or domain, because aggregate metrics hide per-segment collapse.',
  trap: 'The scale-specific insight: at 50M documents, the permission and metadata filter does more for latency AND quality than any ranking improvement, because it removes most of the corpus before similarity is computed.',
  tags: ['rag', 'scale'], orig: 13 },

{ id: 'sd04', topic: 'sysdesign', level: 2,
  q: 'Design a code review assistant.',
  lay: 'Give it the diff, the surrounding code it needs to understand the diff, and your team\'s conventions — then have it comment only where it is confident, because a noisy reviewer gets muted.',
  tech: '<b>Context:</b> the diff, plus retrieved related code (definitions of changed symbols, callers, tests) via a code-aware index chunked on AST boundaries; plus the team\'s conventions from a curated document. <b>Model:</b> a strong model for reasoning; consider a cheap first pass that decides whether a file is worth reviewing at all. <b>Output:</b> structured findings with file, line, severity, category and a suggested fix — never prose. <b>Filtering:</b> a confidence threshold and a per-PR comment cap; suppress categories the team has repeatedly dismissed. <b>Verification:</b> where possible, check the claim mechanically (does the symbol exist, does the test actually cover that branch). <b>Feedback loop:</b> record which comments were accepted, dismissed or resolved, and use the dismissal rate per category to tune. <b>Metrics:</b> comment acceptance rate, dismissal rate by category, real bugs found per 100 comments, and reviewer time saved.',
  trap: 'The product failure mode is noise. Ten low-confidence comments per PR trains everyone to ignore the bot within a fortnight. Optimise for precision, not recall, and make the acceptance rate the primary metric.',
  tags: ['code', 'design'] },

{ id: 'sd05', topic: 'sysdesign', level: 3,
  q: 'Design a system that summarises 10,000 documents daily.',
  lay: 'This is a batch job, not a chat feature. Queue everything, use the cheap batch pricing, do the work overnight, and design for the failures a batch of ten thousand guarantees.',
  tech: '<b>Pipeline:</b> a queue of documents; workers that fetch, extract text, chunk if the document exceeds the window, summarise (map-reduce for long documents), validate the output structure, and store. <b>Cost:</b> use the batch API (roughly half price) since it is not interactive; route by length — short documents to a small model, long ones to a larger one. <b>Long documents:</b> map-reduce — summarise sections in parallel, then combine — or extract-then-summarise. <b>Reliability:</b> idempotent processing keyed by document hash, retry with backoff, dead-letter queue, and progress tracking so a restart resumes. <b>Quality:</b> a faithfulness check (entity and number overlap with the source) on all of them, plus an LLM judge on a sample, plus human review of a stratified sample weekly. <b>Scale arithmetic:</b> 10,000 documents at ~8k input and ~500 output tokens each is 80M input and 5M output tokens a day — a real but manageable number that you should quote.',
  trap: 'At batch scale the tail is guaranteed: some documents are 400 MB, some are corrupt, some are in a language you did not plan for. The dead-letter queue and per-document timeouts are not optional extras, they are the difference between a job that finishes and one that hangs.',
  tags: ['batch', 'summarisation'] },

{ id: 'sd06', topic: 'sysdesign', level: 3,
  q: 'Design a multi-tenant AI platform used by 500 enterprise customers.',
  lay: 'Every request has to know whose it is, at every layer — retrieval, cache, memory, logs, budgets. And no customer should be able to affect another\'s performance or bill.',
  tech: '<b>Isolation:</b> tenant id derived from the authenticated session, enforced server-side in retrieval filters, cache keys, memory scope and log partitioning; an automated cross-tenant test in CI. <b>Customisation:</b> per-tenant prompts and configuration; per-tenant LoRA adapters served from one base model with multi-LoRA rather than a model per customer. <b>Quotas:</b> per-tenant rate limits and daily budgets so one customer cannot exhaust capacity or run up someone else\'s bill. <b>Data:</b> separate indexes or namespaces per tenant, which also makes deletion and export straightforward. <b>Noisy neighbours:</b> priority queues and admission control. <b>Compliance:</b> per-tenant data residency, retention and deletion; audit logs scoped per tenant. <b>Operations:</b> per-tenant dashboards, because an aggregate that looks healthy can hide one customer having a terrible week.',
  trap: 'Per-tenant metrics are the operational insight. Aggregate quality and latency hide the customer who is about to churn, and they are the customer who will tell you first.',
  tags: ['multi-tenant', 'platform'] },

{ id: 'sd07', topic: 'sysdesign', level: 2,
  q: 'Design a semantic search feature for an e-commerce catalogue.',
  lay: 'People search for "warm jacket for walking the dog" and for "SKU-44710". You need both to work, plus filters, plus the ability to show why something ranked where it did.',
  tech: '<b>Hybrid is mandatory:</b> BM25 handles SKUs, brand names and exact model numbers; dense handles descriptive queries. Fuse with RRF. <b>Product text</b> for embedding: title plus key attributes plus a curated description — not the whole marketing page, which dilutes the vector. <b>Filters</b> as first-class: category, price, availability, size, shipping. These are constraints, not ranking signals, and they belong in filter context. <b>Business ranking</b> on top of relevance: availability, margin, popularity, personalisation — blended after retrieval, never inside the embedding. <b>Freshness:</b> price and stock change constantly, so keep them out of the index and join at query time or update via a fast path. <b>Evaluation:</b> click-through and conversion, plus a labelled relevance set for offline iteration. <b>Zero-result handling</b> — relax filters progressively and say what you relaxed.',
  trap: 'Never bake price or stock into the embedded text. They change hourly, and re-embedding the catalogue every hour is absurd — keep volatile attributes in metadata and join them at query time.',
  tags: ['search', 'ecommerce'] },

{ id: 'sd08', topic: 'sysdesign', level: 3,
  q: 'Design an agent that can take actions in a financial system.',
  lay: 'Let it read and reason; do not let it press the button. It proposes, deterministic code checks policy, and a human approves anything above a threshold.',
  tech: '<b>Separation:</b> the model proposes a structured action; a deterministic policy engine approves, rejects or escalates. Policy lives in code, not in the prompt. <b>Permissions:</b> least-privilege tools, per-action and per-day limits enforced outside the model, credentials never in context. <b>Idempotency:</b> a deterministic key per logical action so a retry cannot double-execute. <b>Approval:</b> human sign-off above a value threshold, with durable interrupt and checkpointing so the run survives a two-hour wait. <b>Reversibility:</b> a defined compensating action for everything irreversible, and a saga to unwind a partial failure. <b>Audit:</b> the full trace — conversation, retrieved policy, proposal, decision, approver, executed call, result. <b>Testing:</b> a scenario suite including adversarial inputs, policy edge cases and tool failures, asserting on the END STATE. <b>Kill switch</b> that stops all action execution without a deploy.',
  trap: 'The line to state explicitly: the model decides what SHOULD happen; deterministic code decides what DOES happen. Any design where the model can directly move money will eventually be talked into doing so.',
  tags: ['agents', 'safety'], orig: 45 },

{ id: 'sd09', topic: 'sysdesign', level: 2,
  q: 'Design a meeting-notes assistant.',
  lay: 'Transcribe, work out who said what, summarise into decisions and actions rather than a wall of text, and let people correct it — because it will get names wrong.',
  tech: '<b>Pipeline:</b> audio → ASR with speaker diarisation → transcript with speaker labels and timestamps → structured summarisation. <b>Chunking for long meetings:</b> a two-hour meeting exceeds most windows, so summarise in overlapping windows and then combine, carrying forward a running list of entities and decisions. <b>Output structure:</b> decisions, action items with owners and dates, open questions, and a short narrative — structured output, not prose, so it can be pushed into a task tracker. <b>Grounding:</b> every action item cites a timestamp so it can be verified. <b>Correction loop:</b> let users edit, and feed corrections back as a glossary of names, projects and acronyms that improves both ASR and summarisation. <b>Privacy:</b> consent, retention policy, and the ability to exclude a participant or a segment.',
  trap: 'Proper nouns are the recurring failure — names, product names, acronyms. A per-organisation glossary fed into both the ASR and the summariser fixes more perceived quality than any model upgrade.',
  tags: ['speech', 'summarisation'] },

{ id: 'sd10', topic: 'sysdesign', level: 3,
  q: 'Design the evaluation infrastructure for an AI product team.',
  lay: 'A place to keep test cases, something that runs them against any version of the system, somewhere to store the results, and a view that compares two runs. Plus a queue where humans look at the disagreements.',
  tech: '<b>Case store</b> — versioned datasets with inputs, expected outputs or rubrics, and tags for intent, difficulty and language. <b>Runner</b> — concurrent, cost-capped, retries, records the full configuration (model, prompt, index versions) with each run. <b>Scorers</b> — deterministic assertions, retrieval metrics, judges with pinned models and prompts. <b>Result store</b> — every run queryable and comparable, with per-case drill-down. <b>Comparison view</b> — diff two runs at the case level, not just the aggregate; the cases that flipped are the interesting ones. <b>CI integration</b> — a fast subset gating merges, the full suite nightly. <b>Human queue</b> — judge/assertion disagreements and low scores routed for review, and reviewed cases flow back into the case store. <b>Production sampling</b> — a continuous stream of live traffic scored with the same scorers.',
  trap: 'The case-level diff is the feature that makes it useful. An aggregate that moved from 0.82 to 0.84 tells you nothing; a list of the six cases that got worse tells you exactly what to look at.',
  tags: ['eval', 'platform'], orig: 29 },

{ id: 'sd11', topic: 'sysdesign', level: 2,
  q: 'Design a personalised content recommendation feature using LLMs.',
  lay: 'Use the cheap classical machinery to narrow millions of items to a hundred, and only then use the expensive model — to explain, re-rank or write the pitch.',
  tech: '<b>Funnel:</b> candidate generation with collaborative filtering and embedding retrieval (millions → thousands, milliseconds); a ranking model (thousands → hundreds); then the LLM on the top slice for reranking with reasoning, generating explanations, or writing personalised copy. <b>Never</b> put the LLM in the candidate-generation stage — it cannot score a million items and it does not need to. <b>User representation:</b> a compact profile (interests, recent behaviour, stated preferences) rather than raw history in the prompt. <b>Caching:</b> precompute recommendations for inactive users offline and serve from cache — they are most of your users and none of your engagement. <b>Cold start:</b> content-based embeddings for new items, onboarding preferences for new users. <b>Feedback loop risk:</b> the system only learns about what it showed, so include exploration or the catalogue narrows.',
  trap: 'The feedback loop is the interesting failure: items never shown get no clicks, look bad, and are never shown. Nothing errors; the product just quietly becomes more boring. Deliberate exploration is the fix.',
  tags: ['recsys', 'design'] },

{ id: 'sd12', topic: 'sysdesign', level: 3,
  q: 'Design a system that answers questions over both documents and a database.',
  lay: 'Work out first whether the question is a lookup or a calculation. Lookups go to document search; anything involving counting or filtering goes to the database as a query.',
  tech: '<b>Router first:</b> classify the intent — document lookup, structured query, or both. This is the design. <b>Document path:</b> hybrid retrieval, rerank, grounded answer with citations. <b>Structured path:</b> text-to-SQL against a read-only connection with a schema description, allowlisted tables, query validation before execution, a row limit and a timeout; return the query alongside the result so it can be checked. <b>Hybrid questions</b> ("how many refunds last quarter and what is the policy?") decompose into both paths and synthesise. <b>Security:</b> row-level security in the database, never a tenant filter in the generated SQL. <b>Evaluation:</b> separate sets for each path — SQL correctness is exact-match on results; document answers need faithfulness.',
  trap: 'An LLM cannot reliably sum a column in a prompt. Any question involving aggregation must go to SQL or a calculator tool, and recognising that is the whole point of the router.',
  tags: ['sql', 'rag'] },

{ id: 'sd13', topic: 'sysdesign', level: 2,
  q: 'Design an AI feature under a hard 500 ms latency budget.',
  lay: 'Half a second rules out most of the obvious design. You will need a small model, a cached prompt, no reranker on the critical path, and a very short answer.',
  tech: 'Budget breakdown: network 30 ms, retrieval 50 ms, prefill 100 ms, decode ~300 ms for a short answer. That forces: a small fast model (or a fine-tuned small one); prompt caching so prefill is near zero; retrieval that is a single fast lane, not a fan-out with a cross-encoder; structured short output rather than prose; guardrails running in parallel rather than in series; and co-location to keep network cost down. If the task genuinely cannot be done in the budget, the honest design is asynchronous: return immediately and deliver the full answer moments later, or show a fast partial answer and refine it.',
  trap: 'Say when the budget is not achievable. A design that promises 500 ms and delivers 1,200 ms is worse than one that says "the full-quality path is 1.2 seconds; here is a 400 ms partial answer while it runs".',
  tags: ['latency', 'design'], orig: 33 },

{ id: 'sd14', topic: 'sysdesign', level: 3,
  q: 'Design a system to keep RAG answers fresh when documents change hourly.',
  lay: 'Make everything event-driven and make every cached answer carry the version of the documents it came from — so when a document changes, its answers stop matching automatically.',
  tech: '<b>Ingestion:</b> CDC or webhooks, content hashing to skip unchanged text, idempotent upserts, delete propagation, and a reconciliation job. <b>Versioning:</b> every chunk carries a document version and a valid-from timestamp; retrieval filters to current. <b>Caching:</b> the answer cache key includes a hash of the retrieved chunk ids plus their versions — invalidation becomes naming rather than bookkeeping. <b>TTL by volatility:</b> minutes for pricing and availability, days for policy and definitions. <b>Freshness SLO:</b> measure lag from source change to retrievable, alarm on it, and expose it internally. <b>User-facing:</b> show the source and its date; offer a "get a fresh answer" action. <b>Escape hatch:</b> for genuinely live data (stock, status, balance) do not index it at all — call the API as a tool.',
  trap: 'The distinction that matters: documents belong in the index, live state belongs in a tool call. Trying to keep an index fresh for data that changes by the second is a losing race you do not need to run.',
  tags: ['freshness', 'rag'], orig: 34 },

{ id: 'sd15', topic: 'sysdesign', level: 2,
  q: 'Design an AI writing assistant embedded in an existing editor.',
  lay: 'It has to be fast, it has to know what the user is working on, and it has to be easy to reject. Anything that interrupts or takes two seconds gets turned off.',
  tech: '<b>Latency is the product</b> — inline suggestions need sub-500 ms, which means a small model, prompt caching on the document prefix, and short outputs. <b>Context:</b> the surrounding text, the document outline, and retrieved style or terminology guidance — assembled with a stable prefix so caching works. <b>Interaction:</b> suggestions must be trivially dismissible and never destructive; accept/reject telemetry is your primary quality metric. <b>Streaming</b> so text appears immediately. <b>Personalisation:</b> a per-user or per-organisation style guide, either retrieved or as a small LoRA. <b>Privacy:</b> the document is sensitive; be explicit about what leaves the device and offer a local or zero-retention option for regulated customers. <b>Metrics:</b> acceptance rate, edit distance after acceptance, and time-to-first-suggestion.',
  trap: 'Acceptance rate is the metric, not a quality score. Users vote with the tab key, and edit-distance-after-acceptance tells you whether they accepted it and then had to fix it.',
  tags: ['product', 'latency'] },

{ id: 'sd16', topic: 'sysdesign', level: 3,
  q: 'Design an offline evaluation and online monitoring loop that actually closes.',
  lay: 'Bugs found in production become test cases; test cases gate deploys; deploys are watched in production. If any of those three links is missing, the loop does not close and you fix the same thing repeatedly.',
  tech: '<b>Production → cases:</b> sample live traffic (stratified), route low scores and judge/assertion disagreements into a human review queue, and every confirmed problem becomes an eval case BEFORE it is fixed. <b>Cases → gate:</b> the eval suite runs in CI; a fast subset gates merges and the full suite gates deploys. <b>Deploy → production:</b> canary with pre-declared guardrails and automatic rollback; the model, prompt and index versions are recorded on every request. <b>Measurement:</b> chart the eval score across releases to catch slow drift, and chart production quality metrics daily. <b>Ownership:</b> someone reviews the queue weekly, or the loop silently stops.',
  trap: 'The link that breaks in practice is the human review queue. Everyone agrees it should exist; nobody owns it; three months later it has eight hundred items and no one has read any of them. Name the owner.',
  tags: ['eval', 'process'], orig: 29 },

{ id: 'sd17', topic: 'sysdesign', level: 2,
  q: 'Design a system to extract structured data from unstructured documents at scale.',
  lay: 'Turn invoices, contracts or forms into rows in a table. The model does the reading; your code does the checking, because a confidently wrong invoice total is worse than no invoice total.',
  tech: '<b>Pipeline:</b> classify the document type → route to a type-specific extraction prompt and schema → constrained decoding so the output is valid by construction → validate values (does the total equal the sum of the line items, is the date plausible, does the supplier exist in our system) → route low-confidence extractions to human review. <b>Confidence:</b> field-level, from a combination of logprobs, cross-field consistency checks and agreement between two extractions. <b>Human in the loop:</b> a review UI showing the source region alongside the extracted value, with corrections feeding a fine-tuning set. <b>Cost:</b> batch API, and route simple documents to a small model. <b>Metrics:</b> field-level precision and recall, straight-through-processing rate, and cost per document processed without review.',
  trap: 'Straight-through-processing rate is the business metric — the percentage of documents that need no human touch. Field accuracy of 95% sounds good and can still mean 40% of documents need review, because a document has twenty fields.',
  tags: ['extraction', 'design'] },

{ id: 'sd18', topic: 'sysdesign', level: 3,
  q: 'Design a system where 10 different teams build LLM features on shared infrastructure.',
  lay: 'Give them a paved road: one gateway, shared caching and observability, an evaluation harness, and guardrails they get for free. Let them own their prompts and their retrieval.',
  tech: '<b>Platform provides:</b> a model gateway (routing, fallbacks, rate limiting, cost attribution per team, provider abstraction); shared caching (prompt and semantic); observability with tracing and dashboards out of the box; an evaluation harness and case store; guardrail services (PII, injection, toxicity); and a vector store with tenancy. <b>Teams own:</b> prompts, retrieval logic, eval cases, and their product metrics. <b>Governance:</b> per-team budgets and alerts; a model allowlist with a review process for new ones; a shared registry of prompts and their versions. <b>The hard part is adoption</b> — a platform teams route around is worse than no platform, so make the paved road genuinely faster than the alternative.',
  trap: 'Cost attribution per team is the feature that makes the gateway worth mandating. Without it, finance sees one enormous bill and nobody can act on it; with it, every team sees their own number and behaviour changes.',
  tags: ['platform', 'org'] },

{ id: 'sd19', topic: 'sysdesign', level: 2,
  q: 'Design an AI feature for a market where quality varies by language.',
  lay: 'Test each language separately, be honest about which ones you support well, and route the weak ones differently rather than pretending the average is the experience.',
  tech: '<b>Measure per language</b> — a separate eval set for each supported language, never an aggregate. <b>Retrieval:</b> a genuinely multilingual embedding model, or per-language indexes with query routing; language-appropriate normalisation and analyzers; a multilingual reranker if you use one. <b>Chunking</b> sized in characters rather than tokens for non-Latin scripts, because tokenisers inflate them. <b>Generation:</b> instructions in English often work better even for non-English output, but say explicitly "respond in the user\'s language"; few-shot examples in the target language. <b>Routing:</b> where quality is measurably worse, route to a stronger model, add a translation step, or degrade to human handoff. <b>Product honesty:</b> state which languages are supported at what quality rather than claiming all of them.',
  trap: 'An average across languages hides a collapse. If Arabic is at 30% and English at 85%, the headline "78%" is a number that will end in a customer escalation from your second-biggest market.',
  tags: ['multilingual', 'product'], orig: 41 },

{ id: 'sd20', topic: 'sysdesign', level: 2,
  q: 'Design the caching architecture for a high-traffic LLM product.',
  lay: 'Layers. Normalise the question and check for an exact match. Keep the top of the prompt identical so the provider\'s cache hits. Add a meaning-based layer with a threshold you measured. And put the version of everything into the key.',
  tech: '<b>Layer 1 — normalised exact:</b> lowercase, collapse whitespace, strip punctuation, hash. ~3 ms, always correct, a few percent hit rate on human traffic. <b>Layer 2 — prompt/prefix cache:</b> stable content at the top of the prompt; the provider skips prefill. Large TTFT and cost win, no correctness risk. <b>Layer 3 — semantic:</b> embed the normalised question, search past questions, serve above a tuned threshold. 25–50% hit rate on FAQ traffic, and the only layer that can be wrong. <b>The key:</b> normalised question + tenant + locale + user tier + prompt version + model id + hash of retrieved chunk ids and versions. <b>Guards:</b> negative caching with short TTL (penetration), single-flight (stampede), TTL jitter (avalanche), hot-key replication, and never cache a degraded or unverified answer. <b>Metrics:</b> hit rate AND wrong-answer rate per layer, cost saved weighted by request cost.',
  dgm: { nodes: [{ t: 'normalise' }, { t: 'exact', s: '~3 ms' }, { t: 'semantic', s: '~30 ms', k: 'alt' }, { t: 'prefix cache', s: 'skips prefill', k: 'alt' }, { t: 'model', s: 'full cost', k: 'warn' }],
    cap: 'Each layer catches what the previous one missed; only the semantic layer can serve a wrong answer.' },
  trap: 'Content-versioned keys are the idea worth leading with: they turn cache invalidation into a naming problem, which is the easy problem.',
  tags: ['caching', 'design'], orig: 1 },

{ id: 'sd21', topic: 'sysdesign', level: 3,
  q: 'Your AI feature is successful and traffic grows 10× in a month. What breaks first?',
  lay: 'Rate limits, then cost, then the vector index, then whatever you never load-tested. And your on-call rota.',
  tech: 'In roughly the order it happens: <ol><li><b>Provider rate limits</b> — you hit the ceiling and start returning 429s. Fix: quota increase, throttle at source, second provider, routing to smaller models.</li><li><b>Cost</b> — a bill that was tolerable becomes a board-level conversation. Fix: caching, routing, output caps — but you should have measured cost per request already.</li><li><b>Latency under queueing</b> — you pass the capacity knee and p95 goes vertical. Fix: admission control, more replicas, separate interactive and batch pools.</li><li><b>The vector index</b> — memory or shard limits, and rebuild times that no longer fit in a maintenance window.</li><li><b>The ingestion pipeline</b> — falls behind, freshness lag grows.</li><li><b>Human processes</b> — the review queue, the escalation path, the on-call rota.</li></ol>',
  trap: 'The last one is the one people omit and it is often the real bottleneck. A 10× increase in escalations needs 10× the support capacity, and no amount of engineering fixes that in a month.',
  tags: ['scale', 'ops'] },

{ id: 'sd22', topic: 'sysdesign', level: 2,
  q: 'Design an AI feature where being wrong is expensive — medical, legal or financial.',
  lay: 'Design it to refuse rather than guess, cite everything, and put a qualified human between the model and the consequence. Then measure how often it should have refused and did not.',
  tech: '<b>Scope narrowly</b> — a specific, well-defined task, not a general assistant. <b>Ground everything</b> — retrieval from an authoritative, versioned corpus; no parametric answers. <b>Verify mechanically</b> — every citation exists, every number appears in a source, the output parses. <b>Refuse by default</b> on low confidence, empty retrieval or out-of-scope intent, and measure the refusal rate in both directions. <b>Human review</b> before anything consequential reaches a user, with the reviewer recorded. <b>Audit</b> — pinned model version, full trace, reproducible months later. <b>Evaluation</b> — domain experts, not crowdworkers, with inter-annotator agreement measured; an adversarial set of questions the system should refuse. <b>Product framing</b> — present it as a research assistant that surfaces sources, not as an answer engine.',
  trap: 'The strongest design decision is often reframing the product: a system that finds and summarises the three relevant clauses, with citations, is enormously useful and carries a fraction of the liability of one that gives an answer.',
  tags: ['safety', 'design'] }

]);
